# Copiar a config/push.env (no commitear).
# Firebase Console → Project settings → Cloud Messaging → Server key (legacy API).

FCM_SERVER_KEY=
PUSH_WEBHOOK_SECRET=

# Aliases usados por supabase/config.toml [edge_runtime.secrets]
MOTOLINK_FCM_SERVER_KEY=
MOTOLINK_PUSH_WEBHOOK_SECRET=
