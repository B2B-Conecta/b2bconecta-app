# Supabase Dashboard → Authentication → URL Configuration
# Project staging: lwrqjpqyitnveizshawc
#
# Canonical staging web origin (keep in sync with config/env/staging.env and Vercel):
#   https://motolink-app.vercel.app
#
# Site URL (staging):
#   https://motolink-app.vercel.app
#
# Redirect URLs — add all that apply:
#   https://motolink-app.vercel.app/**
#   http://localhost:3000/**
#   http://127.0.0.1:3000/**
#
# Móvil (APK / iOS — deep link, ver mobile-staging.env):
#   com.carlosf12.motolinkProApp://auth-callback
#
# Production (when motolink.app DNS is live):
#   https://motolink.app/**
#
# Password recovery on web uses PKCE: the email link must open in the SAME browser
# (same origin) where the user clicked "Forgot password". See README → Staging web.
