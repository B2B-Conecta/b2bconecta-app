# Supabase Dashboard → Authentication → URL Configuration
#
# Keep ALL of these Redirect URLs enabled on each project so web recovery
# can use the current browser origin (Uri.base.origin) for PKCE.
#
# === MAIN (b2bconecta-db / fzugzjcwdzcwfxgviltw) — production ===
# Site URL:
#   https://www.b2bconecta.com.ve
# Redirect URLs (keep enabled):
#   https://www.b2bconecta.com.ve/**
#   https://b2bconecta-app-git-dev-b2bconecta.vercel.app/**
#   http://localhost:3000/**
#   http://127.0.0.1:3000/**
#   ve.com.b2bconecta.app://auth-callback
#
# === DEV (b2bconecta-db-dev / kdrccmqcrruixuworlmz) — data de trabajo ===
# Site URL (Vercel branch `dev`):
#   https://b2bconecta-app-git-dev-b2bconecta.vercel.app
# Redirect URLs (keep enabled):
#   https://b2bconecta-app-git-dev-b2bconecta.vercel.app/**
#   https://www.b2bconecta.com.ve/**
#   http://localhost:3000/**
#   http://127.0.0.1:3000/**
#   ve.com.b2bconecta.app://auth-callback
#
# === SMTP (confirmación + recovery) ===
# Sender: b2bconecta.ve@gmail.com
# Dashboard → Authentication → Emails → SMTP Settings
#   o: bash scripts/configure_supabase_smtp.sh staging|production
# Ver config/smtp.env.example
#
# Password recovery on web uses PKCE: open the email link in the SAME browser
# (and same origin) where "Forgot password" was clicked.
#
# Ver config/ENVIRONMENTS.md
